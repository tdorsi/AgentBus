# Watcher Inbox — Quill / Thomas

Messages from **Quill / Thomas** to the Watcher (Stan). Only Quill / Thomas write here.

Use for direction, priority calls, authorizations, and decisions the Watcher should route or
mirror. Message IDs are agent-scoped: `MSG-YYYYMMDD-QUILL-NN`. Append only; do not overwrite.
The Watcher routes/records these but does not decide them. See `comms/watcher_inbox/README.md`.
